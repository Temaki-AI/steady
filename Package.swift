// swift-tools-version: 5.9
// This is for reference only. Open the .xcodeproj in Xcode instead.
// The app requires iOS frameworks (SwiftUI, SwiftData, WidgetKit) that
// Swift Package Manager cannot build directly as an iOS app.

import PackageDescription

let package = Package(
    name: "SteadyFlow",
    platforms: [.iOS(.v17)],
    products: [],
    targets: []
)
